apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-service
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"  # infrastructure 이후 배포
spec:
  project: wealist
  source:
    repoURL: https://github.com/OrangesCloud/wealist-project-advanced-k8s.git
    targetRevision: k8s-deploy-dev
    path: k8s/helm/charts/auth-service
    helm:
      valueFiles:
        - values.yaml
        - ../../environments/dev.yaml
      parameters:
        - name: image.tag
          value: "service-deploy-dev-ci-afb273e"  # ← CI가 여기 업데이트
        - name: image.repository
          value: "ghcr.io/orangescloud/auth-service"
  destination:
    server: https://kubernetes.default.svc
    namespace: wealist-dev 
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
