{{- if .Values.grafana.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: {{ .Values.global.namespace }}
  labels:
    app: grafana
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: monitoring
spec:
  replicas: {{ .Values.grafana.replicas }}
  # Recreate strategy required for PVC-based deployments to avoid lock conflicts
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
        app.kubernetes.io/name: grafana
      annotations:
        # Sidecar 모드 제외 - 모니터링 인프라는 mesh 외부에서 동작
        sidecar.istio.io/inject: "false"
        # ConfigMap 변경 시 자동 Pod 재시작을 위한 checksum
        checksum/config: {{ include (print $.Template.BasePath "/grafana/configmap.yaml") . | sha256sum }}
        checksum/dashboards: {{ include (print $.Template.BasePath "/grafana/configmap-dashboards.yaml") . | sha256sum }}
    spec:
      securityContext:
        fsGroup: 472
        runAsGroup: 472
        runAsNonRoot: true
        runAsUser: 472
      containers:
        - name: grafana
          image: "{{ .Values.grafana.image.repository }}:{{ .Values.grafana.image.tag }}"
          imagePullPolicy: {{ .Values.grafana.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            {{- if .Values.externalSecrets.enabled }}
            # Production: ExternalSecret으로 생성된 Secret에서 가져옴
            - name: GF_SECURITY_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: wealist-shared-secret
                  key: GRAFANA_ADMIN_USER
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wealist-shared-secret
                  key: GRAFANA_ADMIN_PASSWORD
            {{- else }}
            # Local/Dev: values에서 직접 가져옴
            - name: GF_SECURITY_ADMIN_USER
              value: {{ .Values.grafana.admin.user | quote }}
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-secret
                  key: password
            {{- end }}
            - name: GF_USERS_ALLOW_SIGN_UP
              value: {{ .Values.grafana.config.allowSignUp | quote }}
            # Sub-path configuration for Ingress access
            - name: GF_SERVER_ROOT_URL
              value: {{ .Values.grafana.config.rootUrl | default "http://localhost:3000" | quote }}
            - name: GF_SERVER_SERVE_FROM_SUB_PATH
              value: {{ .Values.grafana.config.serveFromSubPath | default "false" | quote }}
            {{- if .Values.grafana.oauth.google.enabled }}
            # =============================================================================
            # Google OAuth Configuration
            # =============================================================================
            - name: GF_AUTH_GOOGLE_ENABLED
              value: "true"
            - name: GF_AUTH_GOOGLE_ALLOW_SIGN_UP
              value: "true"
            - name: GF_AUTH_GOOGLE_AUTO_LOGIN
              value: {{ .Values.grafana.oauth.google.autoLogin | default false | quote }}
            - name: GF_AUTH_GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: wealist-shared-secret
                  key: GOOGLE_CLIENT_ID
            - name: GF_AUTH_GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wealist-shared-secret
                  key: GOOGLE_CLIENT_SECRET
            - name: GF_AUTH_GOOGLE_SCOPES
              value: "openid email profile"
            - name: GF_AUTH_GOOGLE_AUTH_URL
              value: "https://accounts.google.com/o/oauth2/v2/auth"
            - name: GF_AUTH_GOOGLE_TOKEN_URL
              value: "https://oauth2.googleapis.com/token"
            - name: GF_AUTH_GOOGLE_API_URL
              value: "https://openidconnect.googleapis.com/v1/userinfo"
            {{- if .Values.grafana.oauth.google.allowedDomains }}
            - name: GF_AUTH_GOOGLE_ALLOWED_DOMAINS
              value: {{ .Values.grafana.oauth.google.allowedDomains | quote }}
            {{- end }}
            {{- if .Values.grafana.oauth.google.roleAttributePath }}
            - name: GF_AUTH_GOOGLE_ROLE_ATTRIBUTE_PATH
              value: {{ .Values.grafana.oauth.google.roleAttributePath | quote }}
            {{- end }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 5
          resources:
            {{- toYaml .Values.grafana.resources | nindent 12 }}
          volumeMounts:
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            - name: dashboards-provider
              mountPath: /etc/grafana/provisioning/dashboards
            - name: dashboards-json
              mountPath: /etc/grafana/provisioning/dashboards/json
            - name: dashboards-v2
              mountPath: /etc/grafana/provisioning/dashboards/v2
            {{- if .Values.grafana.dashboardsV3.enabled }}
            - name: dashboards-v3-otel-native
              mountPath: /etc/grafana/provisioning/dashboards/v3/otel-native
            - name: dashboards-v3-application
              mountPath: /etc/grafana/provisioning/dashboards/v3/application
            - name: dashboards-v3-logs
              mountPath: /etc/grafana/provisioning/dashboards/v3/logs
            - name: dashboards-v3-slo
              mountPath: /etc/grafana/provisioning/dashboards/v3/slo
            {{- end }}
            - name: data
              mountPath: /var/lib/grafana
      volumes:
        - name: datasources
          configMap:
            name: grafana-datasources
        - name: dashboards-provider
          configMap:
            name: grafana-dashboards-provider
        - name: dashboards-json
          configMap:
            name: grafana-dashboards-json
        - name: dashboards-v2
          configMap:
            name: grafana-dashboards-v2
        {{- if .Values.grafana.dashboardsV3.enabled }}
        - name: dashboards-v3-otel-native
          configMap:
            name: grafana-dashboards-v3-otel-native
        - name: dashboards-v3-application
          configMap:
            name: grafana-dashboards-v3-application
        - name: dashboards-v3-logs
          configMap:
            name: grafana-dashboards-v3-logs
        - name: dashboards-v3-slo
          configMap:
            name: grafana-dashboards-v3-slo
        {{- end }}
        - name: data
          {{- if .Values.grafana.persistence.enabled }}
          persistentVolumeClaim:
            claimName: grafana-pvc
          {{- else }}
          emptyDir: {}
          {{- end }}
---
{{- if .Values.grafana.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  namespace: {{ .Values.global.namespace }}
  labels:
    app: grafana
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.grafana.persistence.storageClass }}
  storageClassName: {{ .Values.grafana.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.grafana.persistence.size }}
{{- end }}
{{- if not .Values.externalSecrets.enabled }}
---
# Local/Dev 환경용: values에서 직접 Secret 생성
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: {{ .Values.global.namespace }}
  labels:
    app: grafana
type: Opaque
data:
  password: {{ .Values.grafana.admin.password | b64enc | quote }}
{{- end }}
{{- end }}
