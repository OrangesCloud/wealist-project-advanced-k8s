# =============================================================================
# Secrets & Credentials
# =============================================================================
# K8s secrets (use .example files as templates)
k8s/base/namespace-dev/secret.yaml
k8s/base/namespace-prod/secret.yaml
services/*/k8s/base/secret.yaml

# TLS certificates
*.pem
docker/scripts/dev/certs/*.pem

# Environment files with secrets
.env
.env.local
.env.*.local
docker/env/.env.dev
docker/env/.env.prod

# =============================================================================
# IDE & OS
# =============================================================================
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# =============================================================================
# Build outputs
# =============================================================================
*.log
tmp/
temp/

# =============================================================================
# Keep these files
# =============================================================================
!*.example
!README.md


# ------------------------------------
# 1. Gradle/JVM 빌드 관련 파일 무시
# ------------------------------------
# Gradle Wrapper 실행 파일 관련
!gradle/wrapper/gradle-wrapper.jar

# 모든 모듈의 .gradle 폴더 (캐시, 로그, 실행 기록)
**/.gradle/

# 모든 모듈의 build 폴더 (컴파일된 클래스, JAR 파일 등 출력물)
**/build/

# 임시 파일 및 캐시
**/tmp/
**/out/

# 컴파일된 클래스 파일 (IDE가 생성)
**/bin/
**/*.class
**/*.jar
**/*.war